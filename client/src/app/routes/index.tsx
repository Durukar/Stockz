import { BrowserRouter, Route, Routes } from "react-router"

const RouterApp = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/">
        
        </Route>
      </Routes>
    </BrowserRouter>
  )
}

export {RouterApp}